# /data (LOCAL, no subir CSV)

- **Qué va aquí**: datasets originales de Kaggle.
- **Archivos esperados** (no se suben al repo): `train.csv`, `test.csv`.
- **Punto(s)**: 1 (cargar cada dataset en su propio dataframe).

> Mantén los CSV **solo** en `/data/raw/`. El repositorio ya ignora esta carpeta.
